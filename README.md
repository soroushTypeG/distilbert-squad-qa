# distilbert-squad-qa

دموی سوال-جواب (Question Answering) با مدل **DistilBERT** که روی زیرمجموعه‌ای از دیتاست **SQuAD v1.1** fine-tune شده است.

این پروژه شامل آموزش مدل، ذخیره وزن‌ها، بارگذاری کانتکست‌ها از دیتابیس SQLite و رابط کاربری تعاملی با Gradio می‌باشد.

## ویژگی‌های پروژه
- مدل پایه: **distilbert-base-uncased-distilled-squad**
- Fine-tuning روی **SQuAD v1.1** (train: ۵۰۰۰ نمونه تصادفی، eval: ۱۰۰۰ نمونه)
- رابط کاربری ساده و تعاملی با **Gradio**
- لود کانتکست‌ها از دیتابیس **SQLite**
- آموزش و inference کاملاً جدا شده
- مدل نهایی روی Hugging Face آپلود شده (بدون نیاز به آموزش مجدد)

## نتایج ارزیابی (روی ۱۰۰۰ نمونه validation)
- **F1 Score**: ۸۲.۴۵  
- **Exact Match**: ۷۰.۱۰

## لینک مدل fine-tune شده
مدل کامل و آماده استفاده روی Hugging Face:  
🔗 https://huggingface.co/soroshXAI/dilstilbert-squad-qa

## نصب و اجرا

### پیش‌نیازها
- Python 3.8 یا بالاتر
- Git

### مراحل راه‌اندازی
1. ریپوزیتوری رو clone کنید:
   ```bash
   git clone https://github.com/soroshXAI/distilbert-squad-qa.git2
   cd distilbert-squad-qa
                                                                                                                                                                                                                                                                                 2. محیط مجازی بسازید (توصیه می‌شود):
   python -m venv venv
venv\Scripts\activate  # ویندوز
# یا
source venv/bin/activate  # لینوکس / مک
3. وابستگی‌ها رو نصب کنید: 
pip install -r requirements.txt
4. دمو رو اجرا کنید:
python app.py
مرورگر شما باز می‌شود:
http://127.0.0.1:7860

## ساختار پروژه ##
.
├── app.py                # رابط کاربری Gradio + inference
├── train.py              # اسکریپت آموزش مدل (فقط یک بار لازم است)
├── database.py           # لود کردن کانتکست‌ها از دیتابیس
├── populate_db.py        # ساخت و پر کردن دیتابیس نمونه
├── requirements.txt      # لیست پکیج‌های مورد نیاز
├── .gitignore            # جلوگیری از آپلود فایل‌های غیرضروری
└── README.md             # این فایل توضیحات

آموزش مدل (اختیاری)
فایل train.py برای fine-tuning مدل روی SQuAD استفاده می‌شود:

فقط یک بار لازم است اجرا شود (اگر می‌خواهید مدل را از اول آموزش دهید).
زمان تقریبی: ۳۰–۷۵ دقیقه روی GPU T4 (Colab).
مدل نهایی در ./trained_model ذخیره می‌شود.

نکته مهم:
برای راحتی و جلوگیری از آموزش مجدد، مدل آموزش‌دیده قبلاً روی Hugging Face آپلود شده است و app.py به صورت پیش‌فرض از آنجا لود می‌کند.
اگر می‌خواهید خودتان دوباره آموزش دهید:
bash
python train.py
لایسنس
MIT License – آزاد برای استفاده، تغییر و توزیع
این پروژه بخشی از کارهای شخصی برای یادگیری و نمایش مهارت‌هایم است.
هر پیشنهادی برای بهبود خوشحال می‌شوم بشنوم!
✨ ساخته شده با Python, Transformers و کمی عشق به NLP
